#include<bits/stdc++.h>
using namespace std;

int main(){

    map<int, int> mp;
    // map store the key and value pair 
    // where each key are unique in map and they are sorted by the key.



    multimap<int, int> mmp;
    // while the multimap also store the key value pair but the multimap allows 
    // the duplication of the key. 
    // it  also store the pair in sorted order of the key.

    // you can refer to gfg to learn more basics about the multimaps.
    
    return 0;
}